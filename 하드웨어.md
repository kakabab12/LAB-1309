# 장비와 사양별 주의사항

## 로봇팔 — AgileX PiPER

| 항목 | 내용 |
|---|---|
| 구성 | **6축 + 그리퍼** → 동작 7차원 |
| 통신 | **CAN** (CAN-USB 어댑터 필요) |
| SDK | `piper_sdk` (Python), ROS/ROS2 지원 |
| 제어 방식 | 관절 제어, 끝단(EE) 제어 모두 가능 |
| 데이터 수집 | 리더-팔로워 원격조종 또는 드래그 티칭 |
| LeRobot 연동 | 위고로보틱스 공개 플러그인 `WeGo-Robotics/lerobot_robot_piper` |

**플러그인 요구 사항**
- Linux + SocketCAN
- Python 3.10 이상, LeRobot 0.3.0 이상
- `iproute2`, `ethtool`, `can-utils`
- CAN-USB 인터페이스(gs_usb)

**확인할 것**
- [ ] 리더 팔(원격조종용)이 있는가
- [ ] CAN-USB 어댑터가 있는가
- [ ] 로봇에 연결할 Ubuntu PC가 있는가

## 카메라

| 위치 | 용도 | 비고 |
|---|---|---|
| 정면(3인칭) | 작업 공간 전체 | 스탠드로 고정, 위치를 표시해둘 것 |
| 손목 | 잡는 순간 근접 확인 | 로봇 끝단에 브래킷 고정 |

**세팅 원칙**
- **자동 초점·자동 노출 끄기** (프레임마다 밝기가 변하면 학습이 흔들림)
- 640×480 / 30fps면 충분 (모델 입력은 더 작게 줄어듦)
- 두 카메라는 서로 다른 USB 포트에 (대역폭)
- **데이터 수집 때와 실행 때 위치가 같아야 함**

## GPU

| GPU | VRAM | 세대 | bf16 | 용도 |
|---|---|---|---|---|
| **GTX 1080 Ti** | 11GB | Pascal (cc 6.1) | ❌ | 추론, LIBERO 평가, 전환 실험, **저사양 기준점** |
| **RTX 4070 Ti** | 12GB | Ada (cc 8.9) | ✅ | SmolVLA 파인튜닝 |

### ⚠️ 1080 Ti 주의사항

| 항목 | 내용 |
|---|---|
| PyTorch 2.8+ cu128/cu129 | ❌ Pascal 지원 제거됨 |
| CUDA 13 | ❌ Turing 이상만 지원 |
| **권장 조합** | **PyTorch 2.7.1 + cu126** (2.10.0+cu126도 동작 확인 — 2026-09-14) |
| 정밀도 | bf16 불가 → **fp32로 동작** (메모리↑ 속도↓) |
| FlashAttention | 미지원 |

설치:

```bash
pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu126
```

확인 (기대: `True (6, 1)`):

```bash
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_capability())"
```

### 모델별 대략적인 요구 사양

| 모델 | 크기 | 추론 | 파인튜닝 |
|---|---|---|---|
| ACT | 약 80M | 어디서나 | 8GB에서도 가능 |
| **SmolVLA** | 0.45B | 수 GB | 배치 8에서 약 10~16GB |
| VLA-Adapter | 0.5B 백본 | 작음 | LoRA 배치 1에서 약 9.6GB |
| TurboVLA | 0.2B | 1GB 미만 | 논문은 4090 4장 |
| π0 | 3B | 8GB 이상 | LoRA 약 22.5GB |
| OpenVLA | 7B | 양자화 필요 | 24GB로도 어려움 |

> 수치는 공개 문서·논문 기준의 대략값입니다. **실제로는 직접 측정해서 기록**하세요.

## 소프트웨어 버전 기록

잘 돌아가는 조합을 찾으면 여기에 적어두세요.

| 날짜 | OS | Python | PyTorch | LeRobot | GPU | 비고 |
|---|---|---|---|---|---|---|
| 2026-09-14 | Ubuntu (6.8 커널) | 3.10.12 | 2.10.0+cu126 | 0.4.4 | GTX 1080 Ti (드라이버 580.178.04) | LIBERO-Goal + SmolVLA 추론 정상. hf-libero 0.1.4, MuJoCo 3.8.1, robosuite 1.4.0, transformers 4.57.6. `MUJOCO_GL=egl`, 추론 VRAM 약 2.2GB |

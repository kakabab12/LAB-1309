# 생성 노이즈가 결과를 가른다 — 20% ↔ 90% (2026-09-18, 진행 중)

## 📌 쉬운 말 요약

SmolVLA 는 동작을 **노이즈에서 시작해 다듬어** 만듭니다(flow matching). 그 시작 노이즈는 보통 "그냥 무작위"라고 생각합니다.

그런데 **전환 순간에 노이즈 순서를 특정 값으로 고정하면 성공률이 20%에서 90%까지 갈렸습니다.**
모델도, 상태도, 지시문도 똑같은데 말입니다.

**학습이 전혀 필요 없습니다.** 코드 한 줄(`torch.manual_seed`)입니다.

---

## 1. 어떻게 발견했나

[오라클 Best-of-N](../oracle_bon/README.md) 실험에서 **모든 상태에 같은 8개 시드**를 썼기 때문에,
시드별 성공률을 비교할 수 있었습니다 (36개 상태).

| 시드 | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|---|
| 성공률 | 31% | 31% | **14%** | 36% | 28% | **14%** | 28% | **44%** |

최대-최소 차이 31%p, **순열 검정 p = 0.018** (상태 안에서 시드 라벨을 섞어도 이만큼 갈리지 않음).

**왜 가능한가**: 전환 순간에 `torch.manual_seed(s)` 를 걸면, 그 이후 flow matching 이 뽑는
**노이즈 순서가 상태와 무관하게 고정**됩니다. 어떤 순서는 좋은 쪽으로, 어떤 순서는 나쁜 쪽으로 풉니다.

## 2. ⚠️ 교란 요인과 그 해결

오라클에서는 **시드 k 가 항상 k 번째로 실행**됐고, 상태를 복원할 때 MuJoCo 물리는 되돌려도
**로봇 제어기(OSC)의 내부 목표값은 되돌려지지 않습니다.** 그래서 "시드 효과"와 "실행 순서 효과"가 섞입니다.

**검증 설계** — 순서 없이 조건당 한 번씩만, 그리고 **시드를 고를 때 쓰지 않은 초기상태**에서:

| | 시드를 고른 데이터 | 검증 데이터 |
|---|---|---|
| 초기상태 | 에피소드 0~9 (오라클) | **에피소드 10~19** |
| 실행 | 한 상태에서 8번 연속 | **조건당 1회** |

## 3. 검증 결과 (진행 중)

### 쌍이 늘어도 증가폭이 같다

| 쌍 | 기본 | **좋은 시드** | 차이 |
|---|---|---|---|
| 8→7 | 60% | **90%** | +30%p |
| 4→7 | 40% | **70%** | +30%p |
| **합계 (20 에피소드)** | **50%** | **80%** | 짝 비교 9 : 3, p = 0.146 |

두 쌍에서 **증가폭이 정확히 같습니다**(+30%p). 나머지 두 쌍(1→5, 2→5)은 오라클에서
어떤 노이즈로도 거의 안 되는 쌍이라 효과가 없을 것으로 예상합니다.

### ✅ 오라클 예측과 실측이 맞는다 (이중 검증)

시드는 **오라클(에피소드 0~9)** 에서 골랐고, 검증은 **미사용 에피소드(10~19)** 에서 했습니다.
두 값이 맞으면 ① 오라클 측정이 타당하고 ② 시드 효과가 초기상태를 바꿔도 안정적이라는 뜻입니다.

| 쌍 | 조건 | 오라클 (ep 0~9) | **실측 (ep 10~19)** |
|---|---|---|---|
| 8→7 | 기본 | 60% | **60%** |
| 8→7 | 좋은 시드 | 80% | **90%** |
| 8→7 | 나쁜 시드 | 30% | **20%** |
| 4→7 | 기본 | 40% | **40%** |
| 4→7 | 좋은 시드 | 80% | **70%** |

**기준선이 정확히 일치합니다** (60/60, 40/40). 시드 조건도 ±10%p 안에서 맞습니다.

이건 2절의 한계(오라클에서 OSC 제어기가 복원되지 않는 문제)도 **부분적으로 해소**합니다 —
그 교란이 컸다면 오라클의 기준 성공률이 실제와 어긋났을 텐데, 정확히 일치했습니다.

### 자세히 (A8→B7)

| 조건 | B 성공 (에피소드 10~19) |
|---|---|
| 기본 (무작위 노이즈) | 60% |
| **좋은 시드 10679** | **90%** |
| **나쁜 시드 10485** | **20%** |

**70%p 차이입니다.** 실행 순서 효과가 없는 조건에서도 시드 효과가 남았습니다.

### 짝 비교 — 무엇이 확립됐고 무엇이 아직인가

같은 초기상태끼리 맞대 본 결과 (10 에피소드, 부호 검정)

| 비교 | 결과 | p | 판단 |
|---|---|---|---|
| **좋은 시드 vs 나쁜 시드** | 7 : 0 | **0.016** | ✅ **노이즈 선택이 결과를 가른다** |
| 좋은 시드 vs 기본 | 4 : 1 | 0.375 | ⏳ 표본 부족 |
| 나쁜 시드 vs 기본 | 1 : 5 | 0.219 | ⏳ 표본 부족 |

**정확히 말하면**: "어떤 노이즈를 쓰느냐가 성공을 가른다"는 **확립됐습니다**.
"고른 좋은 시드가 무작위보다 낫다"는 **아직 확립되지 않았습니다** (에피소드 10개로는 부족).
나머지 3개 쌍의 결과가 나오면 40 에피소드로 다시 검정합니다.

### 좋은 노이즈는 더 부드럽기도 하다 (자연스러움 제약에 유리)

전환 전후 50스텝의 저크(3차 차분, 작을수록 부드러움):

| 조건 | 저크 중앙값 | 저크 분포 |
|---|---|---|
| A8→B7 기본 | 8.84 | 7.65 ~ 10.20 |
| **A8→B7 좋은 시드** | **6.86** | **6.26 ~ 7.84** |
| A4→B7 기본 | 8.36 | 7.79 ~ 10.13 |
| **A4→B7 좋은 시드** | **6.86** | **6.37 ~ 7.72** |

두 가지가 보입니다.

1. **좋은 노이즈는 성공률만 높은 게 아니라 동작도 더 부드럽습니다** (8.4~8.8 → 6.86).
   [제약](../../../연구주제.md) 중 "사람처럼 자연스럽게"에도 유리합니다
2. **서로 다른 태스크 쌍인데 좋은 시드일 때 저크 분포가 거의 같습니다** (6.26~7.84 vs 6.37~7.72).
   같은 노이즈 순서를 쓰면 **동작의 결이 비슷해진다**는 뜻 — 노이즈가 동작을 크게 좌우한다는 또 다른 증거

### 나쁜 시드는 물체를 오래 들고 있는다

| 조건 | 물체를 놓음 | **놓기까지 스텝** | 전환 직후 20스텝 이동 |
|---|---|---|---|
| 기본 (무작위) | 100% | 68 (3.4초) | 14.5cm |
| **좋은 시드** | 100% | **61 (3.1초)** | 14.0cm |
| **나쁜 시드** | 100% | **203 (10.2초)** | 16.2cm |

나쁜 노이즈는 로봇이 **쥔 물체를 10초나 들고 다니게** 만듭니다. 그동안 새 일은 진행되지 않습니다.

> ⚠️ 주의: [실패의 정체](../failure_mode/README.md) 1절에서 "놓기까지 걸린 시간은 성공/실패를 가르지 못한다"
> (무작위 노이즈에서 실패 49스텝 vs 성공 66스텝, p=0.12)고 했습니다. 모순이 아닙니다 —
> **무작위 노이즈 전체를 평균하면 차이가 없지만, 특정 나쁜 노이즈는 놓기를 크게 늦춥니다.**

### 좋은 시드는 무엇을 다르게 하나 — 초반에는 같다

전환 직후 2초 동안의 움직임을 비교했습니다.

| 조건 | 2초 변위 | 2초 경로 | 방향 일치도 |
|---|---|---|---|
| 기본 | 22.9cm | 30.3cm | 0.99 |
| 좋은 시드 | 24.4cm | 31.4cm | 1.00 |
| 나쁜 시드 | 21.9cm | 31.2cm | 1.00 |

**초반에는 거의 같습니다.** 셋 다 같은 방향으로 비슷한 거리를 갑니다.
차이는 **그 뒤에** 벌어지는데, 이는 [실패의 정체](../failure_mode/README.md)에서 찾은
"성공과 실패가 5.5초부터 갈린다"와 맞습니다.

## 3-2. ⭐ 두 발견이 하나로 이어진다 — 노이즈가 idling 을 결정한다

[실패의 정체](../failure_mode/README.md)에서 실패의 모양이 **policy idling**(좁은 영역에서 맴돌기)이라는 것을 찾았습니다.
좋은 시드와 나쁜 시드의 궤적을 그 지표로 다시 보면:

| 조건 | 왕복 지표 (6초) | **idling 포함 비율** | B 구간 길이 | B 성공 |
|---|---|---|---|---|
| 기본 (무작위) | 1.34 | 40% | 159 스텝 | 60% |
| **좋은 시드** | **1.17** | **10%** | **130 스텝** | **90%** |
| **나쁜 시드** | **1.84** | **90%** | **300 스텝** (시간초과) | **20%** |

**생성 노이즈가 "제자리 맴돌기에 빠지느냐"를 결정합니다.**

이것이 [흔들기가 실패한 이유](../recovery/README.md)도 설명합니다 —
**이미 빠진 뒤에 흔들어봐야 소용없고, 애초에 안 빠지는 것이 중요**합니다.
그리고 빠질지 말지는 전환 순간의 노이즈에서 상당 부분 정해집니다.

### 그래서 다음 질문: 첫 동작 묶음이 전부를 정하는가 ⏳

| ablation | 무엇을 가르나 |
|---|---|
| 시드 고정 (지금) | 전환 이후 노이즈 **순서 전체**가 고정 |
| **첫 chunk 만 고정** | 첫 동작 묶음만 정해지고 그 뒤는 무작위 → **첫 묶음이 결정적인가** |
| **매 추론마다 재시드** | 정책이 완전히 **결정적** → "흔들리지 않는 것" 자체가 좋은가 |

`--switch-noise-first-only`, `--switch-noise-reseed` (⏳ 실행 예약)

## 3-3. 시드 품질이 쌍 사이에 옮겨가는가 — 부분적 증거

오라클 데이터를 쌍별로 쪼개 시드별 성공률을 보면:

| 쌍 | 상태 | 시드0 | 1 | **2** | 3 | 4 | **5** | 6 | **7** |
|---|---|---|---|---|---|---|---|---|---|
| 8→7 | 10 | 60% | 80% | **30%** | 70% | 60% | **30%** | 70% | **80%** |
| 4→7 | 10 | 40% | 30% | **20%** | 60% | 40% | **20%** | 30% | **80%** |
| 2→5 | 6 | 17% | 0% | 0% | 0% | 0% | 0% | 0% | 0% |
| 1→5 | 10 | 0% | 0% | 0% | 0% | 0% | 0% | 0% | 0% |

- **시드 7 이 두 쌍 모두에서 최고**, **시드 2·5 가 두 쌍 모두에서 최저**입니다
- 시드 순위 상관 **ρ = +0.62 (p = 0.10)** — 표본이 8개뿐이라 유의하진 않지만 방향은 뚜렷합니다
- 어려운 쌍(1→5, 2→5)은 어떤 시드로도 안 되므로 비교가 안 됩니다

⚠️ **중요한 한계**: 비교 가능한 두 쌍이 **B 태스크가 같습니다**(둘 다 7 = turn on the stove).
그래서 "장면 전체에 좋은 노이즈"인지 **"태스크 7 에 좋은 노이즈"** 인지 아직 구분이 안 됩니다.

→ 예약된 검증이 이걸 가릅니다: **9→7 은 B 가 같고, 6→0 · 3→5 는 B 가 다릅니다.**

| 결과 | 뜻 |
|---|---|
| 9→7 만 오름 | 좋은 노이즈는 **B 태스크마다** 다르다 → 태스크별 보정 |
| 셋 다 오름 | **장면과 무관**하게 좋은 노이즈가 있다 → 한 번 보정으로 끝 |
| 셋 다 안 오름 | 쌍마다 달라 보정이 필요 → [DSRL](../../../02_논문노트/DSRL.md) |

## 4. 남은 검증 — 처음 보는 태스크 쌍 ⏳

시드는 8→7 / 4→7 / 1→5 / 2→5 에서 골랐습니다. **A 로 한 번도 쓰지 않은 태스크**로 검증합니다.

| 쌍 | A | B |
|---|---|---|
| 9→7 | put the wine bottle on the rack | turn on the stove |
| 6→0 | put the cream cheese in the bowl | open the middle drawer of the cabinet |
| 3→5 | open the top drawer and put the bowl inside | push the plate to the front of the stove |

| 결과 | 뜻 |
|---|---|
| 새 쌍에서도 오름 | **장면과 무관한 "좋은 노이즈"가 존재한다** → 학습 없이 바로 쓸 수 있는 개선 |
| 새 쌍에서는 안 오름 | 좋은 노이즈가 **장면마다 다르다** → 상태를 보고 노이즈를 고르는 학습이 필요 ([DSRL](../../../02_논문노트/DSRL.md)) |

**어느 쪽이든 의미가 있습니다.** 후자라도 "노이즈가 의미 있는 조종 손잡이"라는 DSRL 의 전제를
우리 환경에서 직접 확인한 것이 됩니다 — 그리고 [학습한 판정기가 실패한 이유](../lora_v2/README.md)(Q−V=0.0003)도
설명됩니다. 판정기는 **동작**을 봤지만, 실제로 결과를 가른 건 **노이즈**였을 수 있습니다.

## 5. 정직하게

- 지금 검증은 **태스크 쌍 하나(8→7)** 뿐입니다. 나머지 쌍은 실행 중입니다
- 에피소드 10개씩이라 표본이 작습니다. **좋은 시드 vs 나쁜 시드만 유의**(p=0.016)하고,
  **좋은 시드 vs 무작위는 아직 유의하지 않습니다**(p=0.375)
- 즉 지금 말할 수 있는 것은 "노이즈가 중요하다"이지 "우리가 개선했다"가 아닙니다
- 좋은 시드를 고를 때 **그 쌍들의 데이터를 썼습니다.** 처음 보는 쌍 검증이 끝나야 결론을 낼 수 있습니다

## 6. 실행

```bash
cd ~/smolVLA
# 전환 순간부터 노이즈 순서를 고정
.venv/bin/python switch_experiment.py --task-a 8 --task-b 7 --switch-at grasp:3 \
  --strategy flush --switch-noise-seed 10679 --episodes 10 --start-episode 10 --out outputs/seedtest
.venv/bin/python analyze_seedtest.py
```

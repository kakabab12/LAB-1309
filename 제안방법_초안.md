# 제안 방법 초안 (2026-09-18)

> 지금까지의 측정 결과를 하나로 묶은 **방법 설계 초안**입니다.
> ✅ = 측정으로 뒷받침됨, ⏳ = 실험 진행 중, ❓ = 아직 확인 안 됨 (추측)
>
> 배경과 제약: [연구주제.md](연구주제.md) · 결과 요약: [실험 목차](04_실습/05_libero/README.md)

---

## 0. 한 문단 요약

SmolVLA 는 물체를 쥔 상태에서 새 지시를 받으면 32% 만 성공하고(✅), 중단한 원래 일은 **0%** 로 못 돌아옵니다(✅).
초기 자세로 복귀시키면 89% / 62% 로 뛰지만(✅) **그건 리셋이라 금지**입니다.
우리는 ① **쥔 물체를 집었던 자리에 되돌려 놓되 초기 자세로는 가지 않는** 동작(rollback)을 정의하고,
② 그 동작을 **LoRA 로 정책에 증류해 추론 때 스크립트를 없애고**,
③ 저사양 추론 지연(실측 0.56초, ✅)에 대응하는 **비대칭 동작 묶음**을 함께 씁니다.

## 1. 문제를 다시 정의하면

우리가 풀어야 하는 것은 세 조건을 **동시에** 만족시키는 것입니다.

| 조건 | 왜 어려운가 | 근거 |
|---|---|---|
| **A) 새 지시를 해낸다** | 물체를 쥔 상태는 어떤 태스크의 학습 데이터에도 없는 상태 | flush 32% ✅ |
| **B) 중단한 일로 돌아온다** | 쥐고 있던 물체가 아무 데나 떨어져 낯선 배치가 됨 | 재개 0% ✅ |
| **C) 초기 자세로 가지 않고, 사람처럼 이어서 움직인다** | A·B 를 쉽게 푸는 방법(리셋)이 바로 이 조건을 깬다 | retreat 는 38cm 더 돌아감 ✅ |

**선행 연구는 셋 중 하나씩만 풉니다.**

| | A (새 지시) | B (재개) | C (리셋 금지·자연스러움) |
|---|---|---|---|
| [SwitchVLA](02_논문노트/SwitchVLA.md) | ✅ 50.9% (LIBERO mid) | ❌ 되돌려 없앰 | ❌ advance 가 기준 자세로 보간 |
| [ReSteer](02_논문노트/ReSteer.md) | ✅ +11%p | ❌ | ❌ 다른 시연 상태로 보간 |
| [RTC](02_논문노트/Real-Time-Chunking.md) | ❌ 전환엔 손해 | ❌ | ✅ 매끄러움 |
| **우리 목표** | ✅ | ✅ | ✅ |

## 2. 왜 지금까지의 방법이 다 실패했나 (측정으로 배제한 것들)

```
전환 실패의 원인이 무엇인가?

① 팔이 학습 분포에서 멀어서       → ❌ r = +0.14 (거의 무관)          ✅측정
② B 의 정상 경로에서 멀어서        → ❌ r = +0.23, 단조성 없음          ✅측정
③ 남은 동작(chunk) 처리 방식       → ❌ flush/keep/blend/rtc 모두 30%대  ✅측정
④ 후보 동작 중 못 골라서           → ❌ 판정기 Q−V = 0.0003             ✅측정
⑤ 데이터가 부족해서               → ❌ 442 에피소드 학습해도 32→31%     ✅측정
⑧ 물체를 안 놓고 버텨서            → ❌ 성공·실패 모두 100% 놓음         ✅측정
⑨ 엉뚱한 방향으로 가서             → ❌ 방향은 맞고 60~80% 까지 감        ✅측정
⑩ **좁은 영역에서 왕복(idling)**    → ✅ **실패의 80% (성공 22%)**        ✅측정
⑥ 그 상태에서 지시가 동작에 영향을
   못 줘서 (instruction-blind)     → ⏳ CMI 측정 중
⑦ 후보 자체가 존재하지 않아서      → ⏳ 오라클 Best-of-N 측정 중
```

**⑤가 특히 중요합니다.** [SwitchVLA 부록 C](02_논문노트/SwitchVLA.md) 도 같은 것을 보고했습니다 — 전환 시연을 50~100개 더 모아도 효과가 미미했습니다.
**전환은 "데이터를 더 모으면 되는 문제"가 아닙니다.** 무엇을 해야 하는지(모드)를 가르쳐야 합니다.

## 2-2. ⭐ 지금까지 알아낸 인과 사슬 (2026-09-18 오후)

```
물체를 쥔 채 지시가 바뀐다
        ↓
전환 순간에 뽑히는 **생성 노이즈**가 앞으로의 진행을 크게 좌우한다
   (같은 상태·같은 지시인데 좋은 시드 90% / 나쁜 시드 20%)
        ↓
나쁜 쪽으로 풀리면 **좁은 영역에서 맴돌기(policy idling)** 에 빠진다
   (나쁜 시드 idling 90% / 좋은 시드 10%)
        ↓
한번 빠지면 **흔들어도 못 나온다** (4방향 전부 실패, PIP 원본 포함)
        ↓
초기 자세로 완전히 돌아가야 살아난다 (89%) — 그런데 그건 금지
```

**그래서 개입해야 할 지점은 "빠진 뒤"가 아니라 "전환 순간의 노이즈"입니다.**

## 3. 제안 — 세 부분 (⚠️ 아래 3-1·3-5 는 측정 결과 실패, 3-6 이 새 주 방향)

> ## 📍 2026-09-18 저녁 기준 — 방향 정리
>
> **닫힌 길** (측정으로 배제)
>
> | 방향 | 결과 |
> |---|---|
> | 3-1 `rollback` | ❌ 19% vs flush 32% (p=0.064) |
> | 3-5 흔들기 4방향 | ❌ 25~31%, 기준선과 차이 없음 (PIP 원본 포함) |
> | `finish_a` (하던 일 마치고) | ❌ 20~30%, 팔이 더 멀어짐(34cm) |
> | **회복(recovery) 전체** | ❌ **통하는 지점(초기 위치 7cm 안)은 자연스러움 지표에서 리셋과 구분 안 됨** |
> | 3-6 ① 고정 좋은 노이즈 | ❌ 처음 보는 쌍에서 80%→20% (p=0.03) |
>
> **열린 길**
>
> | 방향 | 근거 | 상태 |
> |---|---|---|
> | **장면별 노이즈 보정** | 고를 때 쓴 쌍에서는 +30%p | ⏳ 6→0 에서 검증 중 (`run_calib.sh`) |
> | **DSRL (상태→노이즈 학습)** | 노이즈가 양방향 60~70%p 를 움직임. 상한 28→58% | ❓ 다음 후보 |
> | **역커리큘럼 학습** (정책 자체를 바꾸기) | 회복이 닫혔으므로 남은 유일한 길 | ❓ 데이터 수집 경계 결과 필요 |
>
> 아래 3-1·3-5 의 설계 근거는 **기록용으로 남겨 둡니다** — 왜 그렇게 생각했는지가 다음 판단에 필요합니다.

> ## ⚠️ 2026-09-18 오후 — 3-1 과 3-5 는 **측정 결과 실패**했습니다
>
> | 제안 | 결과 | 자세히 |
> |---|---|---|
> | 3-1 `rollback` | ❌ **19%** vs flush 32% (p=0.064, 더 나쁨) | [회복 실험](04_실습/05_libero/recovery/README.md) |
> | 3-5 흔들기 4방향 | ❌ 25~31%, 기준 25% 와 차이 없음 (p≥0.63) | 〃 |
>
> **부분적으로 초기 자세에 가까워지는 것은 효과가 없습니다.** 초기 위치에 **도착**해야 살아납니다
> (ret_pos 64%, retreat 89%). 남은 실험은 **"얼마나 가까이 가야 하는가"** 입니다 (f = 0.25/0.5/0.75).
> 아래 3-1·3-5 의 설계 근거는 **기록용으로 남겨 둡니다** — 왜 그렇게 생각했는지가 다음 판단에 필요합니다.

### 3-1. rollback: 되돌려 놓되 리셋하지 않는다 ❌ (측정 결과 실패)

```
지금까지 실행한 동작:  a₁ a₂ … a_g … a_t        (a_g = 그리퍼를 닫은 시점)
rollback 이 실행:      −a_t … −a_g … −a_{g−10}   (순서 뒤집고 부호 바꿈)
                              ↑ 이 시점부터 그리퍼를 연다 = 물체를 그 자리에 놓음
```

- 동작이 **끝단 delta**(위치 3 + 회전 3 + 그리퍼)이므로, **순서를 뒤집고 부호를 바꾸면 수학적으로 정확한 역동작**입니다 (회전도 `(R₁R₂…Rₙ)⁻¹ = Rₙ⁻¹…R₁⁻¹`)
- 되돌리는 범위를 **집기 시점 −10스텝**, 최대 **60스텝**으로 묶으므로 초기 자세까지 갈 수 없습니다 → **제약 C 만족**
- 끝나는 자리는 "물체를 집기 직전" = **원래 학습 분포 안** → 제약 A 에 유리 ([ReSteer](02_논문노트/ReSteer.md) 의 "분포가 겹치는 상태")
- ~~물체가 원래 자리에 세워져 놓임 → 재개에 유리~~ → ⚠️ **전제가 틀렸음** (2026-09-18):
  `retreat` 는 물체를 원래 자리로 되돌리지 않습니다. 전환 지점에서 내려놓고 **팔만** 초기 자세로 갑니다.
  재개가 되는 이유는 **팔이 초기 자세로 가서**입니다

> **근거를 한 번 수정했습니다 (2026-09-18).** 처음에는 "물체를 놓게 만들려고" rollback 이 필요하다고 생각했는데,
> 궤적을 확인해 보니 로봇은 **이미 100% 놓습니다** (성공·실패 모두). 문제는 **어디에 놓는지**입니다.
> 지금은 아무 데나 놓아서 A 재개가 0% 입니다. 그래서 rollback 의 주된 기대 효과는
> **B 성공보다 A 재개**입니다.
- 검증 지표: `home_dist_after_cm` (초기 위치와의 거리 — 크면 리셋이 아님), 자연스러움 4지표

**이건 [SwitchVLA](02_논문노트/SwitchVLA.md) 의 rollback 모드와 같은 발상입니다.** 다른 점은 ① advance 모드(기준 자세로 보간)를 **쓰지 않는다**는 것과 ② 다음 단계에서 **학습으로 내재화**한다는 것입니다.

### 3-2. 증류: 스크립트를 정책 안으로 ❓

rollback 은 지금 **스크립트**입니다. 스크립트가 있으면 ① 실물에서 동작 기록이 필요하고 ② "사람처럼"이 아니라 "기계처럼" 움직이고 ③ 상황에 맞춰 조절되지 않습니다.

```
1단계  rollback 스크립트로 전환 → B 성공한 에피소드를 모음
2단계  그 궤적을 지시문 = B 로 라벨해 LoRA 학습
       (⚠️ 스크립트 구간 R 도 포함해야 함 — 지금까지는 R 을 제외했다)
3단계  학습 후에는 `--strategy flush` 로 평가 → 정책이 스스로 되돌려 놓는지 확인
```

**⚠️ 지금까지의 데이터 수집 원칙을 하나 바꿔야 합니다.** 여태 스크립트 구간(R)은 "그 동작을 배우면 안 된다"고 제외했습니다. 그건 **초기 자세 복귀(retreat)** 를 배우면 안 되기 때문이었습니다. rollback 은 제약을 지키는 동작이므로 **배워도 됩니다.** 이 구분을 데이터 수집 코드에 넣어야 합니다.

**성공 판정**: 학습 후 `flush` 로 평가했을 때 (ㄱ) B 성공률이 오르고 (ㄴ) `home_dist_after_cm` 가 여전히 크고 (ㄷ) 자연스러움 지표가 나빠지지 않아야 합니다.

### 3-3. 비대칭 동작 묶음: 저사양 지연 대응 ⏳

두 요구가 정면으로 충돌합니다 (✅ 둘 다 측정됨).

| | 원하는 것 | 이유 |
|---|---|---|
| 지연 대응 | 묶음을 **길게** | 실측 추론 0.56초. 묶음이 짧으면 계획이 바닥나 로봇이 멈춤 → 성공률 12% |
| 전환 반응 | 묶음을 **짧게** | 지시가 바뀌면 빨리 꺾어야 함 |

**제안**: 평소에는 길게, **지시가 바뀐 직후에만 짧게**.

```
평소:       n_action_steps = 25   (추론 간격 1.25초 > 지연 0.56초 → 계획이 안 끊김)
전환 직후:  즉시 재요청 + 3스텝마다 재추론 (하드웨어가 허용하는 만큼)
            + RTC 로 도착 지연 구간을 이전 동작과 이어붙임 (✅ 지연에서 12→28%)
```

전환 직후에 자주 재추론하는 것은 **오라클 결과의 직접적인 응용**입니다 — 같은 상태·같은 지시인데 생성 노이즈만 달라도 결과가 갈리므로(⏳ 측정 중), 한 후보에 오래 갇히지 않는 것이 이득입니다. **판정기가 필요 없습니다** (판정기는 이미 실패했습니다 ✅).

**선행 연구 중 이 맞바꿈을 잰 것이 없습니다.** ([RTC](02_논문노트/Real-Time-Chunking.md) 는 지연만, [SwitchVLA](02_논문노트/SwitchVLA.md)·[ReSteer](02_논문노트/ReSteer.md) 는 전환만 봅니다.)

### 3-4. ⭐ 좋은 노이즈 고르기 (2026-09-18 발견, ⏳ 검증 중)

오라클 실험 중간 결과에서 뜻밖의 것을 발견했습니다. 모든 상태에 **같은 8개 노이즈 시드**를 썼기 때문에
시드별 성공률을 비교할 수 있었는데, 이렇게 갈렸습니다 (14개 상태 기준).

| 시드 | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|---|
| 성공률 | 50% | 71% | 36% | 71% | 64% | **21%** | 64% | **86%** |

최대-최소 차이 **64%p**, 순열 검정 **p = 0.021**.

**왜 이런 일이 가능한가**

SmolVLA 는 flow matching 으로 동작을 만듭니다. 노이즈에서 출발해 10단계에 걸쳐 동작으로 다듬습니다.
전환 순간에 `torch.manual_seed(s)` 를 걸면 **그 이후 뽑히는 노이즈 순서가 상태와 무관하게 고정**됩니다.

```
노이즈 순서 A  →  "물체를 내려놓고 새 일로 간다" 쪽으로 풀림   → 성공 ❓
노이즈 순서 B  →  "계속 들고 간다" 쪽으로 풀림                → 실패 ❓
```

**사실이면 학습이 전혀 필요 없습니다.** 좋은 시드를 쓰면 됩니다.
그리고 [DSRL](02_논문노트/DSRL.md)(노이즈 공간 강화학습)의 전제가 우리 환경에서 성립한다는 직접 증거입니다 —
노이즈가 단순한 무작위성이 아니라 **의미 있는 조종 손잡이**라는 뜻입니다.

**검증 설계** (같은 데이터로 고르고 검증하면 안 되므로 나눔)

| | 시드를 고른 데이터 | 검증 데이터 |
|---|---|---|
| 초기상태 | 에피소드 **0~9** (오라클) | 에피소드 **10~19** (한 번도 안 씀) |
| 조건 | - | 기본(무작위) / 좋은 시드 / 나쁜 시드 |

**⚠️ 교란 요인 (반드시 가려야 함)**

오라클에서 시드 k 는 **항상 k 번째로 실행**됩니다. 그리고 상태를 복원할 때 MuJoCo 물리 상태는 되돌려도
**로봇 제어기(OSC)의 내부 목표값은 되돌려지지 않습니다.** 그래서 "시드 효과"와 "실행 순서 효과"가
지금 설계에서는 분리되지 않습니다.

검증 실험(에피소드 10~19)은 **순서 없이 조건당 한 번씩만** 돌리므로 이 교란이 없습니다.
거기서도 좋은 시드가 이기면 **진짜 시드 효과**입니다. 안 이기면 순서 artifact 였다는 뜻입니다.

(같은 이유로 오라클의 "기본 성공률"을 `flush` 성공률과 직접 비교할 때도 주의해야 합니다.
오라클 안에서의 기본 vs 오라클 비교는 같은 조건이라 문제없습니다.)

⏳ 실행 예약됨. 검증되면 **다음 두 가지**로 이어집니다.
1. 즉시 적용 가능한 개선 (학습 0)
2. "좋은 노이즈"를 상태에 따라 고르도록 학습 → [DSRL](02_논문노트/DSRL.md) 축소판. 판정기(Q)가 실패한 이유도
   설명될 수 있습니다 — 판정기는 **동작**을 봤지만, 실제로 결과를 가르는 건 **노이즈**였을 수 있습니다

### 3-5. idling 탈출 흔들기 ❌ (측정 결과 효과 없음)

[실패의 정체](04_실습/05_libero/failure_mode/README.md)를 파다가 실패의 모양을 찾았습니다:
로봇은 물체를 놓고(100%) B 쪽으로 가다가(경로의 60~80%) **좁은 영역에서 왕복**합니다.

이 현상에는 이미 이름이 있습니다 — **policy idling** ([PIP](02_논문노트/PIP-Policy-Idling.md), arXiv 2508.15669).
PIP 는 실패의 84.9%(시뮬)·90%(실물)에 idling 이 있다고 보고했고, 우리 `flush` 는 **80%** 입니다 (성공은 22%).

**PIP 의 해결책은 초기 자세 쪽으로 동작을 섞는 것입니다** — 우리 제약과 충돌합니다.
그리고 우리 데이터가 이 긴장을 정확히 보여줍니다.

| 전략 | B 성공 | 성공 에피소드의 idling |
|---|---|---|
| `flush` | 32% | 22% |
| `retreat` (⛔금지) | **89%** | **6%** |

**우리 연구 질문**: **초기 자세를 참조하지 않고 idling 을 깰 수 있는가?**

| 방향 | 초기 자세 참조 | 역할 |
|---|---|---|
| `home` | ⛔ 예 | PIP 원본 — **비교 기준** |
| `lift` | ✅ 아니오 | 위로 들어올리기 |
| `reverse` | ✅ 아니오 | 최근 동작을 거꾸로 |
| `random` | ✅ 아니오 | **대조군** — 방향이 중요한가, 흔들기 자체가 중요한가 |

**PIP 논문은 이 ablation 을 하지 않았습니다.** `random` 이 `home` 만큼 잘 되면
"초기 자세 참조는 불필요하다"는 뜻이고, 우리 제약 안에서 쓸 수 있습니다.

감지는 **끝단 위치**로 합니다 (관절값 대신) — 로봇 종류와 무관하게 쓰려는 설계 원칙 때문입니다.
`--dither-window 40 --dither-range-cm 3.0 --dither-steps 10 --dither-gain 0.5 --dither-cooldown 40`

### 3-6. ⭐ 새 주 방향 — 동작이 아니라 **노이즈**를 조종한다

**근거** (전부 측정됨)

| 사실 | 값 |
|---|---|
| 같은 상태·같은 지시, 노이즈만 다르게 8번 → 성공 개수가 0~7개로 갈림 | [오라클](04_실습/05_libero/oracle_bon/README.md) |
| 시드별 성공률 14~44% (순열검정 p=0.018) | 〃 |
| 미사용 에피소드에서 좋은 시드 90% vs 나쁜 시드 20% (짝 비교 7:0, p=0.016) | [노이즈 시드](04_실습/05_libero/noise_seed/README.md) |
| 좋은 시드는 idling 10%, 나쁜 시드는 90% | 〃 |
| **동작**을 보는 판정기는 실패 (Q−V = 0.0003) | [LoRA 2차](04_실습/05_libero/lora_v2/README.md) |

마지막 줄이 중요합니다. **판정기가 동작을 봐도 결과를 예측 못 한 이유**가, 실제로 결과를 가른 것이
**동작이 아니라 노이즈**였기 때문일 수 있습니다. 같은 노이즈에서 나온 동작들은 겉보기에 비슷하지만
(초반 2초 변위 22~24cm, 방향 일치도 0.99+) 뒤에서 갈립니다.

**방법 후보**

| 단계 | 내용 | 비용 |
|---|---|---|
| ① 고정 좋은 노이즈 | 장면(또는 태스크 쌍)마다 좋은 시드를 보정해 두고 씀 | **학습 0** |
| ② 노이즈 후보 중 고르기 | 전환 순간 노이즈 K개를 뽑아 점수로 고름 | 점수 함수 필요 (동작 기반은 실패했으므로 **노이즈 기반**이어야) |
| ③ [DSRL](02_논문노트/DSRL.md) 축소판 | 상태를 보고 좋은 노이즈를 내놓는 작은 정책을 강화학습 | 약 50만 파라미터, 1080 Ti 가능 |

⏳ ①의 **일반화 검증**(처음 보는 태스크 쌍)이 실행 중입니다. 결과에 따라:

| 결과 | 다음 |
|---|---|
| ~~새 쌍에서도 통함~~ | ❌ **반증됨** (6→0 에서 80%→20%, p=0.03) |
| **새 쌍에서는 안 통함** | ✅ **이쪽이었음** → 장면별 보정(⏳ 검증 중) 또는 ③ DSRL |

**추가로 알아낸 것** (2026-09-18 저녁)

| 사실 | 뜻 |
|---|---|
| 매 추론마다 같은 노이즈 → **0%** (0:10, p=0.002) | 노이즈 **변화 자체가 진행에 필수**. 무작위성은 탈출 장치 |
| 첫 chunk 만 고정 → 29% (기준선과 동일) | 이득은 **노이즈 순서 전체**에서 나온다. 첫 판단이 아니다 |
| 태스크 단독에서도 100→60% / 70→100% | 노이즈 효과는 **전환 특화가 아니라 정책의 일반 성질** |

→ DSRL 설계 시: **한 번의 노이즈가 아니라 노이즈 시퀀스를 조종**해야 하고,
**고정값을 내놓으면 안 됩니다**(0% 가 됨). 상태에 따라 **변화하는** 노이즈를 내놓아야 합니다.

### 3-7. ⭐ 새 주 방법 — 노이즈 **분포의 중심**을 옮긴다 (CEM)

오늘의 세 측정이 설계를 정확히 규정합니다.

| 측정 | 설계에 주는 제약 |
|---|---|
| 노이즈만 바꿔 성공률이 양방향 60~70%p | 노이즈는 **쓸 만한 조종 손잡이다** |
| 고정된 좋은 시드는 장면마다 다르다 (6→0 에서 80→20%) | **고정값을 못 쓴다.** 장면마다 찾아야 한다 |
| **매 추론마다 같은 노이즈 → 0%** | **노이즈를 고정하면 안 된다.** 변화가 진행에 필수 |

세 번째가 핵심입니다. 그래서 노이즈를 **고정하지 않고 분포의 중심만 옮깁니다.**

```
원래 정책:   z ~ N(0, I)                      →  [얼린 SmolVLA]  →  동작
제안:        z = μ + ε,  ε ~ N(0, I)          →  [얼린 SmolVLA]  →  동작
                 ↑ 32차원(동작 차원마다 하나), 모든 시점에 같은 값
```

- **μ = 0 이면 원래 정책과 완전히 같습니다** (안전한 기본값)
- 노이즈의 **변화(ε)는 그대로 남습니다** → "결정적이면 0%" 문제를 피합니다
- 정책 가중치를 건드리지 않으므로 **망각이 없고**, 동작은 여전히 정책이 만들어 **자연스러움이 유지**됩니다

**찾는 방법: CEM** (cross-entropy method). 후보 μ 를 뽑아 평가하고 잘한 것들로 분포를 갱신합니다.
32개 숫자를 찾는 문제라 SAC 보다 훨씬 단순하고 1080 Ti 에 맞습니다.
후보마다 **같은 초기상태**로 평가해 비교 잡음을 줄입니다.

| 항목 | 값 |
|---|---|
| 탐색 차원 | 32 (동작 차원) |
| 세대 | 8, 세대당 후보 6개, 상위 2개로 갱신 |
| 후보당 평가 | 같은 초기상태 6 에피소드 |
| 비용 | 쌍당 약 290 rollout ≈ 2시간 (1080 Ti) |
| 검증 | **미사용 에피소드 10~19** 에서 기준선과 비교 |

⏳ 8→7(기본 60%) 과 6→0(기본 80%) 에서 실행 예약 — 쉬운 쌍에서 먼저 확인합니다
(어려운 쌍은 [오라클에서 상한이 0%](04_실습/05_libero/oracle_bon/README.md) 라 학습 신호가 없습니다).

**성공 판정**: 미사용 에피소드에서 기준선보다 오르고, [자연스러움 지표](04_실습/05_libero/naturalness/README.md)가 나빠지지 않아야 합니다.

**다음 단계**: μ 가 통하면 **상태에 따라 μ 를 내놓는 작은 정책**으로 확장 ([DSRL](02_논문노트/DSRL.md) 축소판).
지금은 μ 가 장면마다 고정이라 새 장면에서는 다시 찾아야 합니다.

### 3-8. ⭐ 하나의 방법으로 다 풀리지 않는다 (2026-09-18 확정)

같은 쌍에서 **서로 다른 처방이 필요하다는 것**이 직접 확인됐습니다.

| 쌍 유형 | 노이즈만 바꾸기 (오라클 상한) | 팔 자세 75% 되돌리기 |
|---|---|---|
| 쉬운 쌍 (8→7, 4→7) | **100%** | 70~80% |
| 어려운 쌍 (1→5, 2→5) | **0% / 17%** | **50~60%** |

- **쉬운 쌍**: 정책이 할 줄 아는데 운이 나빴다 → **노이즈 선택**(CEM/DSRL)으로 충분
- **어려운 쌍**: 그 자세에서는 정책이 못 한다 → **자세**를 바꿔야 한다. 노이즈로는 절대 안 된다

그런데 자세를 바꾸는 유일한 유효 지점(7cm 안)은 [자연스러움 지표에서 리셋과 구분되지 않습니다](04_실습/05_libero/recovery/README.md).

**그래서 최종 설계는 두 갈래를 합쳐야 합니다.**

| 갈래 | 대상 | 방법 | 상태 |
|---|---|---|---|
| ① 노이즈 조종 | 쉬운 쌍 (정책이 할 줄 아는 상태) | CEM → 상태 조건부 μ(s) (DSRL) | ⏳ 실행 중 |
| ② 정책 학습 | 어려운 쌍 (그 자세에서 못 하는 상태) | 낯선 자세에서도 작동하게 학습 (역커리큘럼) | ❓ 데이터 수집 경계 결과 필요 |

②가 어려운 이유: 그 자세에서 성공 궤적을 모을 수 없습니다(오라클 0%).
**단 f=0.75 로는 50~60% 성공하므로, 그 궤적을 "그 자세에서 시작하는 성공 예시"로 쓸 수 있습니다** —
앞부분(자세 복귀 구간)은 버리고 **B 를 수행하는 뒷부분만** 학습하면 제약을 지키면서 능력을 옮길 수 있습니다. ❓

### 3-9. ⭐ 두 번째 길이 다시 열렸다 — 학습 범위를 27cm 까지

[교란 경계 측정](04_실습/05_libero/boundary/README.md)에서 나온 것:

| | |
|---|---|
| 깨끗한 장면에서 25cm 옮겼을 때 | **29%** |
| 실제 전환 (27cm) | **50~55%** ← 임의 교란보다 **21~26%p 쉽다** |
| [LoRA 2차](04_실습/05_libero/lora_v2/README.md)의 학습 교란 범위 | 8~20cm **임의 교란** |

**중요한 함의**: 전환 자세는 "먼데 **자연스러운**" 자세입니다 (태스크를 수행하다 도달했으므로).
임의 교란과 성질이 다릅니다. **LoRA 2차가 임의 교란으로 학습해서 전이가 안 됐을 수 있습니다** —
범위를 넓히는 것보다 **전환 자세 자체로 학습하는 것**이 맞을 수 있습니다.

| 단계 | 내용 |
|---|---|
| 1 | 교란 **20~30cm** 로 데이터 수집 (25cm 에서 29% 성공하므로 가능) — 단 위 함의대로 **전환 자세 수집(`--mode switch`)을 늘리는 것이 더 맞을 수 있다** |
| 2 | 정상 궤적 리허설 + 지시문 균형 (LoRA 2차 설정 그대로 — 망각 방지 검증됨) |
| 3 | 전환 평가. 27cm 가 학습 분포 안에 들어오면 개선될 것 ❓ |
| 4 | 25~30cm 에서 성공률이 0% 면 **역커리큘럼** (20cm 학습 → 25cm 수집 → …) |

**이건 노이즈 조종(3-7)과 독립적인 길입니다.** 둘 다 시도할 가치가 있습니다.

| | 노이즈 조종 (CEM) | 학습 범위 확대 |
|---|---|---|
| 무엇을 바꾸나 | 샘플링 (정책은 그대로) | 정책 자체 |
| 대상 | 쉬운 쌍 (정책이 할 줄 아는 상태) | 모든 쌍 |
| 비용 | 쌍당 2시간, 학습 없음 | 수집 1시간 + 학습 2시간 |
| 위험 | 장면마다 다시 찾아야 함 | 망각 (LoRA 2차 설정으로 완화) |

## 4. 평가 설계

| 지표 | 정의 | 왜 |
|---|---|---|
| B 성공률 | BDDL goal 술어 | 새 지시 수행 |
| **A 재개 성공률** | B 이후 A 지시 → A goal 달성 | **선행 연구에 없는 지표** |
| 둘 다 성공 | B 유지 + A 재개 | 진짜로 원하는 것 |
| `home_dist_after_cm` | 전환 처리 후 끝단과 초기 위치의 거리 | **제약 C 준수 증명** |
| 경로 길이 · 우회 비율 · 멈춤 횟수 · 되돌아감 | [자연스러움](04_실습/05_libero/naturalness/README.md) | 제약 C 를 숫자로 |
| 반응 시간 | 짝지은 `none` 궤적에서 2cm 이상 벌어지는 시점 | 전환이 늦지 않은지 |
| 조종 가능성 (CMI) | 같은 상태에서 지시 M개 × 후보 K개의 분산비 | 진단 |

**조건**: 실측 지연 11스텝(0.56초)을 **기본값**으로 둡니다. 지연 0 은 참고용입니다.
**통계**: 초기상태를 고정해 **짝 비교 부호 검정**. Wilson 신뢰구간.

## 5. 아직 확인 안 된 것 (정직하게)

| ❓ | 어떻게 확인할까 |
|---|---|
| ~~rollback 이 B 성공률을 올리는가~~ | ❌ **19% vs 32%** (p=0.064) |
| ~~rollback 이 A 재개를 끌어올리는가~~ | ❌ **0/14** |
| ~~초기 자세 참조 없이 idling 을 깰 수 있는가~~ | ❌ **4방향 모두 효과 없음** (PIP 원본 포함) |
| **얼마나 가까이 가야 정책이 살아나는가** | ⏳ f = 0.25/0.5/0.75 측정 중 |
| A 재개 실패가 스텝 부족 때문은 아닌가 | ⏳ `--max-steps 600` 통제 실험 |
| 쥐면 지시가 안 들리는가 (instruction-blind) | ⏳ CMI 측정 |
| 좋은 후보가 존재하는가 | ⏳ 오라클 Best-of-N |
| 스크립트를 LoRA 로 증류할 수 있는가 | ❓ rollback 결과를 보고 |
| 물체가 "누워서" 재개가 안 되는가 | ❓ 물체 자세(quaternion) 기록 필요 |
| 실물 PiPER 에서도 되는가 | ❓ 시뮬 결과 확정 후 |

## 6. 위험과 대비

| 위험 | 대비 |
|---|---|
| rollback 이 flush 와 차이 없음 | CMI 결과로 방향 전환: 지시 조건화 강화(프롬프트 수준 접촉 상태), [DSRL](02_논문노트/DSRL.md) |
| 시드 효과가 14개 상태만의 우연 | 40개 상태로 재검정 + 미사용 에피소드 검증 (⏳ 둘 다 예약됨) |
| 증류가 망각을 일으킴 | [LoRA 망각 논문](02_논문노트/LoRA-망각.md) 처방 (r=8, rehearsal, 균형 샘플링) — LoRA 2차에서 효과 확인됨 ✅ |
| 1080 Ti 로 학습이 부족 | 지금까지 LoRA 2500스텝 = 100분, VRAM 2.64GB → 여유 있음 ✅ |
| 실물에서 역동작이 안 통함 (마찰·중력) | 짧은 구간만 되돌리므로 오차 누적이 작음 ❓. 실물에서는 힘 제어 필요할 수 있음 |

# 다른 PC에서 이어받기 — 처음부터 끝까지

> **이 문서 하나만 따라 하면 됩니다.** 앞 맥락을 몰라도 됩니다.
> 2026-09-21 기준. 읽는 데 10분, 환경 만드는 데 20분.
>
> 결과 요약만 보고 싶으면 → [현재상태.md](현재상태.md)

---

# 0부. 이 연구가 무엇인가 (3분)

## 0-1. 한 장면으로

```
로봇이 그릇을 집어 들었다.
그 순간 사람이 말한다: "아니, 그거 말고 스토브를 켜줘."

로봇은 어떻게 해야 하나?
```

우리가 원하는 답:

```
① 들고 있던 그릇을 아무 데나 내려놓고
② 자연스럽게 이어서 스토브를 켜고
③ 다시 원래 하던 일(그릇 옮기기)로 돌아간다
```

**하면 안 되는 답**: 팔을 처음 자세로 되돌린 다음 새로 시작하기.
그건 사람이 "잠깐만" 하고 로봇을 리셋하는 것이지, 로봇이 말을 알아들은 게 아닙니다.

## 0-2. ⛔ 절대 제약 — 이걸 어기면 연구가 무의미합니다

| 제약 | 왜 |
|---|---|
| **초기 자세로 복귀 금지** | 그건 캘리브레이션/리셋이지 전환이 아닙니다 |
| **사람처럼 자연스럽게** | 멈췄다 가는 stop-and-go 금지 |
| 물체를 아무 데나 놓는 것은 **허용** | 제자리에 되돌릴 필요 없습니다 |

⚠️ 코드에 `retreat`, `ret_pos`, `ret_rot` 전략이 있는데 **성적이 제일 좋습니다(89%)**.
**이건 제약 위반입니다.** "얼마나 잘할 수 있는지"의 상한을 재는 용도로만 씁니다.
성적이 좋다고 이걸 방법으로 쓰거나, 학습으로 따라하게 하면 **연구가 무너집니다.**

## 0-3. 지금 성적표 (2026-09-21)

| 전략 | B 성공 | A+B 둘 다 | 제약 |
|---|---|---|---|
| retreat | 89% | 56% | ⚠️ 위반 (상한 참고용) |
| bon | 34% | 0% | ✅ |
| **flush (기준선)** | **32%** | **0%** | ✅ |
| LoRA 1/2/3차 | 32/31/30% | 0% | ✅ |
| rollback | 19% | 0% | ✅ |

**8일 동안 15가지를 시도했는데, 제약을 지키면서 32%를 넘은 것이 하나도 없습니다.**

## 0-4. ⭐ 대신 원인을 찾았습니다

**CMI**(지시문이 동작을 얼마나 가르는가)를 재 봤습니다.

| 언제 | CMI | 뜻 |
|---|---|---|
| 에피소드 시작 | 0.719 | 지시를 잘 듣는다 |
| **물체를 쥔 직후** | **0.284** | **−61%. 귀가 닫힌다** |
| 들고 이동 중 | 0.761 | 다시 열린다 |

**물체를 쥐는 순간, 정책이 일시적으로 지시문에 귀를 닫습니다.** 태스크 4개 모두 같습니다.

지금까지 실패한 대책들은 전부 **"몸"(팔 자세, 생성 노이즈)** 을 고치려 했습니다.
문제는 **"귀"** 였습니다. 팔이 어디 있든 노이즈가 무엇이든, 지시문이 동작에 영향을 못 주면
새 지시를 따를 방법이 없습니다.

---

# 1부. 환경 만들기 (20분)

## 1-1. 필요한 것

| | 최소 | 비고 |
|---|---|---|
| OS | **Linux** | LIBERO 가 Linux 만 지원 |
| GPU | 8GB 이상 | 1080 Ti(11GB)로 검증됨 |
| CPU | 4코어 이상 | 시뮬레이션이 CPU를 많이 씁니다 |
| RAM | 16GB 이상 | |
| 디스크 | 20GB | 모델 + LIBERO 에셋 |

⚠️ **1080 Ti 같은 Pascal 세대는 cu128/CUDA 13 을 못 씁니다.** 반드시 **cu126** 빌드를 쓰세요.
bf16 도 안 되므로 fp32 로 돕니다.

## 1-2. 설치

```bash
# uv 설치 (pip 보다 빠르고 충돌을 알아서 풉니다)
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.local/bin/env

# 작업 폴더
mkdir -p ~/smolVLA && cd ~/smolVLA
uv venv --python 3.10
```

```bash
cd ~/smolVLA
uv pip install torch --index-url https://download.pytorch.org/whl/cu126
uv pip install "lerobot[smolvla]==0.4.4"
uv pip install git+https://github.com/Lifelong-Robot-Learning/LIBERO.git
uv pip install robosuite==1.4.1 matplotlib scipy imageio imageio-ffmpeg
```

자세한 설명과 막히는 지점은 → [실행방법.md](04_실습/05_libero/실행방법.md)

## 1-3. 설치 확인

```bash
cd ~/smolVLA
.venv/bin/python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```

기대 출력: `2.10.0+cu126 True`

## 1-4. 실험 코드 가져오기

⚠️ **중요**: 실험 코드는 이 저장소가 아니라 **연구실 PC의 `~/smolVLA/`** 에 있습니다.
저장소에는 **사본**이 들어 있습니다.

```bash
cd ~
git clone https://github.com/kakabab12/LAB-1309.git
cp LAB-1309/04_실습/05_libero/코드/*.py ~/smolVLA/
cp LAB-1309/04_실습/05_libero/코드/*.sh ~/smolVLA/
chmod +x ~/smolVLA/*.sh
```

## 1-5. 첫 실행 — 잘 도는지 확인

```bash
cd ~/smolVLA
.venv/bin/python switch_experiment.py --task-a 8 --task-b 7 \
  --switch-at grasp:3 --strategy flush --episodes 2 --out outputs/test
```

첫 실행은 모델(약 1GB)과 LIBERO 에셋을 받느라 몇 분 걸립니다.
한 에피소드에 **약 90초** 걸립니다.

기대 출력 (마지막 줄):

```
SUMMARY A8_B7_grasp3_flush {"n": 2, "n_switched": 2, ..., "b_success_rate": 0.5, ...}
```

---

# 2부. 지금 무엇이 돌고 있나 (연구실 PC)

## 2-1. 확인하는 법

```bash
cd ~/smolVLA

# 어떤 큐가 돌고 있나
ps -eo pid,args | awk '$2=="bash" && /run_/{print $1, $3}'

# 지금 실제로 도는 파이썬
ps -eo pid,etime,args | grep '\.venv/bin/python' | grep -v grep

# GPU 상태
nvidia-smi --query-gpu=memory.used,utilization.gpu --format=csv
```

## 2-2. 2026-09-21 12:40 기준 걸려 있는 큐

GPU 가 1개라 **순서대로 이어 달리게** 해 두었습니다. 앞 것이 끝나야 뒤 것이 시작합니다.

| # | 스크립트 | 무엇을 정하나 | 로그 | 예상 종료 |
|---|---|---|---|---|
| 1 | `run_cfg.sh` | 지시문 증폭 w=1.5/2/3 이 전환을 개선하는가 | `outputs/cfg.log` | 13:30 |
| 2 | `run_cfg_cmi.sh` | **증폭이 실제로 CMI를 올리는가** ← 제일 중요 | `outputs/cfg_cmi.log` | 15:30 |
| 3 | `run_cmi_curve.sh` | 귀가 언제 닫히고 언제 열리는가 | `outputs/cmi_curve.log` | 17:00 |
| 4 | `run_manypairs.sh` | 쌍을 4개 → 20개로 | `outputs/manypairs.log` | 21:30 |
| 5 | `run_repeat.sh` | 대안: 지시문 토큰 반복 | `outputs/repeat.log` | 23:30 |

## 2-3. 진행 상황 보기

```bash
grep -E "^===|^SUMMARY" outputs/cfg.log
```

`===` 는 단계 구분, `SUMMARY` 는 한 조건이 끝났다는 뜻입니다.

## 2-4. ⚠️ 새 실험을 걸기 전에

**GPU 가 1개라 동시에 돌리면 서로 느려집니다.** 두 가지 방법이 있습니다.

**방법 A — 뒤에 이어 붙이기 (권장)**

```bash
# 마지막 큐의 PID 를 찾아서
PID=$(ps -eo pid,args | awk '$2=="bash" && /run_repeat\.sh/{print $1; exit}')
nohup ./내스크립트.sh "$PID" > outputs/내로그.log 2>&1 &
```

큐 스크립트들은 **첫 인자로 받은 PID 가 끝날 때까지 기다리도록** 짜여 있습니다.

**방법 B — 지금 것을 멈추기**

```bash
pkill -f run_cfg.sh        # 큐 스크립트를 죽이고
pkill -f switch_experiment  # 실제로 도는 파이썬도 죽입니다
```

⚠️ 큐 스크립트만 죽이면 **파이썬은 계속 돕니다.** 둘 다 죽여야 합니다.

---

# 3부. 결과 읽기

## 3-1. 분석 명령

```bash
cd ~/smolVLA

.venv/bin/python analyze_cfg.py           # 지시문 증폭 (성공률 + jerk + 실패유형)
.venv/bin/python compare_steer.py outputs/steer_base9 outputs/steer_cfg15 outputs/steer_cfg30
.venv/bin/python cmi_curve.py outputs/steer_curve     # CMI 시간 곡선
.venv/bin/python pair_steer.py outputs/steer_curve    # 쌍별 조종 가능성
.venv/bin/python analyze_lora_versions.py             # 원본/1차/2차/3차 비교
.venv/bin/python analyze_boundary.py                  # 교란 경계
```

## 3-2. ⚠️ 판단할 때 반드시 지킬 것

### ① 성공률만 보지 마세요

지시문 증폭은 **동작을 키웁니다** (정규화 크기 0.69 → 0.89(w=1.5) → 1.11(w=2)).
성공률이 올라도 동작이 거칠어지면 **"사람처럼 자연스럽게" 제약을 깹니다.**
`analyze_cfg.py` 가 jerk 를 항상 같이 찍습니다.

### ② 그렇다고 jerk 하나로도 판단하면 안 됩니다

전략 13개의 jerk 를 재 보니 **순서가 거꾸로**였습니다.

| 전략 | jerk (낮을수록 부드러움) | 제약 |
|---|---|---|
| **retreat (초기자세 복귀)** | **3.63** ← 가장 부드러움 | ⚠️ **위반** |
| release | 4.24 | ⚠️ 위반 |
| **flush (그 자리)** | **9.59** | ✅ 준수 |
| rollback | 10.54 | ✅ 준수 |

리셋 동작은 **P 제어로 매끄럽게 보간하는 스크립트**라 사람이 짠 부드러운 궤적입니다.
반대로 정책이 스스로 뽑는 동작은 chunk 마다 재추론하면서 덜컹거립니다.

**jerk 만으로 "자연스럽다"고 주장하면 리셋이 최고라는 결론이 나옵니다** —
우리가 부정하려는 바로 그 결론입니다.

→ **jerk + 초기 자세까지의 거리 + 경로 길이**를 함께 보세요.

### ③ ⭐ 표본이 작으면 아무 말도 하지 마세요

조건당 10회로는 **53%p 미만을 구분할 수 없습니다.**

| 표본 | 검출 가능한 최소 효과 |
|---|---|
| 단일 쌍 10회 | **53%p** |
| 4쌍 합계 36회 | 28%p |
| 4쌍 × 2시점 72회 | 20%p |

(기준 성공률 25%, 단측 α=0.05, 검정력 80%)

**실제로 이것 때문에 주말을 날렸습니다**:

> CEM(노이즈 최적화)이 8→7 에서 **+30%p (10회)** 로 보여서 "통한다"고 판정하고 진행했는데,
> 같은 쌍의 다른 **20회에서 −20%p** 로 뒤집혔습니다. 전체 50회 합산은 **−10%p (p=0.885)**.
> 10회로는 53%p 미만을 못 보니, +30%p 는 애초에 **구분 불가능한 잡음**이었습니다.

**그래서 우리의 "효과 없음" 결론은 전부 "큰 효과 없음"입니다.**

| 이렇게 쓰면 | |
|---|---|
| "LoRA 는 전환을 개선하지 못한다" | ❌ 과한 주장 |
| "LoRA 는 20%p 이상의 개선을 주지 못한다" | ✅ 데이터가 말하는 것 |

---

# 4부. 실험을 빨리 돌리기

## 4-1. GPU 를 바꾸면 빨라지나 — 거의 아닙니다 (2026-09-21 실측)

에피소드 36개의 실제 시간을 재서 쪼개 봤습니다.

| | 스텝당 | 비중 |
|---|---|---|
| 정책 추론 (GPU) | 56 ms | **38%** |
| **시뮬레이션·렌더링 (CPU)** | 92 ms | **62%** |
| 합계 | 148 ms | |

| GPU 를 바꾸면 | 전체 속도 |
|---|---|
| RTX 4070 Ti | 1.37배 |
| RTX 4090 | 1.46배 |
| **추론이 아예 공짜여도** | **1.61배가 한계** |

**LIBERO 시뮬레이션이 CPU 에서 돌기 때문입니다.** 18시간짜리 실험이 12시간 되는 정도입니다.

## 4-2. 대신 병렬 실행 (3~4배, 돈 안 듦)

1080 Ti 11GB 에 SmolVLA 는 약 3GB 라 **3개까지 안전**합니다 (CPU 8코어 기준).

```bash
cd ~/smolVLA
for p in 8:7 4:7 1:5; do
  .venv/bin/python switch_experiment.py --task-a "${p%:*}" --task-b "${p#*:}" \
    --switch-at grasp:3 --strategy flush --episodes 10 --out outputs/switch &
done
wait
```

결과 파일 이름이 쌍마다 달라 **섞이지 않습니다.**

⚠️ **주의**

- **추론 시간을 재는 실험은 절대 병렬로 돌리지 마세요.** 서로 GPU 를 나눠 써서 값이 부풀려집니다.
  2026-09-18 에 실제로 겪었고, 단독으로 다시 재서 560ms 를 얻었습니다
- 같은 `--out` 에 같은 쌍·같은 설정을 두 번 돌리면 파일이 덮어써집니다
- `nvidia-smi` 로 메모리를 보면서 개수를 늘리세요

## 4-3. 실물 로봇(PiPER)에는 GPU 가 필수입니다

1080 Ti 단독 추론 **560ms = 11.2스텝**(20Hz 기준). 그리고 우리 측정:

| 추론 지연 | 태스크 성공률 |
|---|---|
| 0초 | 90% |
| 0.25초 | 72% |
| **0.5초** | **12%** |

**지금 GPU 로는 실물에서 12% 밖에 안 나옵니다.** → [지연 실험](04_실습/05_libero/latency/README.md)

---

# 5부. 다음에 무엇을 하나

## 5-1. 갈림길

체인 2번(`run_cfg_cmi.sh`)이 **증폭이 실제로 CMI 를 올리는지** 잽니다. 결과에 따라:

| CMI | 성공률 | 뜻 | 다음 |
|---|---|---|---|
| ↑ | ↑ | ✅ 방법이 통한다 | 20쌍으로 확대 검증 |
| ↑ | 그대로 | 귀는 열렸는데 행동이 안 따라옴 | **채널이 병목이 아니다** → 가설 수정 |
| 그대로 | 그대로 | 증폭이 채널을 못 키움 | 아래 5-2 순서로 |

**성공률보다 CMI 를 먼저 보세요.** 성공률은 표본이 비싼데(쌍당 100회),
CMI 는 연속값이라 적은 표본으로도 신호가 보입니다.

## 5-2. 증폭이 안 통하면 (순서대로)

1. **지시문 토큰 반복** (`--instr-repeat 3`) — 이미 구현·예약됨. 가장 싸고, 동작이 안 커지고, 추론도 1회
2. **attention 가중치 직접 조정** — 언어 토큰에 가는 주의를 추론 때 키움
3. **CMI 를 목적함수로 학습** — 기존 LoRA 는 **동작을 모방**했지 언어의 역할을 안 건드렸습니다.
   [BayesVLA](02_논문노트/BayesVLA.md) 의 진단("지시문 가짓수가 적어 장면 지름길로 기운다")과 맞물립니다
4. **전환 시점을 늦추기** — CMI 가 회복되는 "들고 이동 중"까지 기다렸다 전환.
   단 즉시성을 포기하는 것이라 문제 정의가 바뀝니다. **기준선으로만** 의미가 있습니다

## 5-3. ❌ 닫힌 길 — 다시 열지 마세요

| 방향 | 최종 결과 |
|---|---|
| 노이즈 — 좋은 시드 고정 | 처음 보는 쌍에서 **7%** (기준 31%, p=0.031) |
| 노이즈 — CEM 으로 평균이동 학습 | 검증 50회 **−10%p** (p=0.885) |
| 노이즈 — 크기 조절 (σ 0.5~2.0) | σ=2.0 에서 전부 0% |
| 노이즈 — 판정기로 후보 선택 | Q−V = 0.0003 |
| 학습 — LoRA 1·2·3차 | 32 / 32 / 31 / 30% |
| 회복 — 흔들기 4방향·rollback·finish_a | 전부 기준선 이하 |
| 회복 — 초기 위치로 되돌리기 | 통하지만(89%) **제약 위반** |

## 5-4. 중단 기준 (미리 정해 둠)

| 시점 | 확인 | 안 되면 |
|---|---|---|
| 2026-09-23 (수) | 증폭·반복이 CMI 를 올리는가 | 둘 다 안 되면 "추론 개입" 방향 폐기 |
| 2026-10-02 (금) | 20쌍에서 기준선을 넘는 방법이 하나라도 있는가 | **방법 추구를 멈추고 진단 중심으로 정리** |

방법이 안 되더라도 **진단(CMI 발견)만으로 내놓을 것이 있습니다.**
그게 아무것도 없이 시간만 가는 것보다 낫습니다.

---

# 6부. 실수하지 않기 위한 목록

실제로 겪은 것들입니다.

| # | 교훈 | 무슨 일이 있었나 |
|---|---|---|
| 1 | **돌아가는 bash 스크립트를 덮어쓰지 말 것** | bash 는 파일을 조금씩 읽습니다. 실행 중 수정했다가 스크립트가 깨졌습니다 |
| 2 | `pgrep -f` 대신 `ps -eo pid,args` | `pgrep -f` 가 **자기 명령줄**과 래퍼 쉘까지 잡아, 대기 스크립트가 스스로를 기다렸습니다 |
| 3 | **한 축만 재고 결론짓지 말 것** | "회전 30도 절벽"이라고 썼는데, 위치+회전 조합 데이터에 한 시간 만에 뒤집혔습니다 |
| 4 | **예측을 먼저 적어 둘 것** | 판정 기준까지 스크립트에 넣어 두니 데이터가 오는 즉시 결론이 났습니다 |
| 5 | **자동 판정에 유의성 조건을 넣을 것** | 점추정만 보고 `cem_works` 로 진행해 주말을 닫힌 길에 썼습니다 |
| 6 | **작은 표본의 +30%p 를 믿지 말 것** | 20회에서 −20%p 로 뒤집혔습니다 |
| 7 | **가설 검증에 맞는 집단인지 먼저 볼 것** | 정상 궤적으로는 "틀어진 손목이 고쳐지는가"를 잴 수 없습니다. 스크립트가 답을 뱉어도 다른 질문의 답입니다 |
| 8 | **새 방법은 항등 검사부터** | 파라미터를 중립값(w=1)으로 두면 원본과 **비트 단위로 같아야** 합니다. 첫 구현에 상쇄 오차가 있었고, 안 잡았으면 나중 차이가 효과인지 버그인지 몰랐습니다 |
| 9 | **추론 시간 측정은 단독으로** | 다른 평가와 GPU 를 공유해 값이 부풀려졌습니다 |
| 10 | **긴 실행은 horizon 을 확인할 것** | `--max-steps 600` × 3구간이 robosuite horizon(1000)을 넘겨 실험이 죽었습니다 |

---

# 7부. 파일 지도

## 7-1. 실험 코드 (`~/smolVLA/`, 사본은 [04_실습/05_libero/코드/](04_실습/05_libero/코드/))

| 파일 | 내용 |
|---|---|
| `switch_experiment.py` | **전환 실험 본체.** 모든 전략이 여기 있습니다 |
| `instr_cfg.py` | 지시문 증폭 — 이전 지시를 음의 조건으로 |
| `test_cfg_identity.py` | w=1 항등 검사 (새 방법 만들 때 본보기) |
| `steerability.py` | CMI 측정 |
| `analyze_steer.py` / `compare_steer.py` | CMI 정리 / 조건 간 비교 |
| `cmi_curve.py` | CMI 시간 곡선 |
| `pair_steer.py` | 쌍별 조종 가능성이 성공률을 예측하는가 |
| `analyze_cfg.py` | 증폭 평가 (성공률 + jerk) |
| `rotation_at_resume.py` | 재개 시점 손목 회전 |
| `rotation_vs_travel.py` | 손목 회전 드리프트 |
| `pose_sensitivity.py` | 자세 교란 (`--save-traj` 로 궤적 저장) |
| `analyze_boundary.py` | 교란 경계 |
| `analyze_lora_versions.py` | 원본/1·2·3차 비교 |
| `oracle_bon.py` / `cem_noise.py` | (닫힌 길) 오라클 / 노이즈 최적화 |

## 7-2. 문서 (이 저장소)

| 필요할 때 | 문서 |
|---|---|
| **지금 상태 한 장** | [현재상태.md](현재상태.md) |
| 연구 배경·제약 | [연구주제.md](연구주제.md) |
| 방법 설계 | [제안방법_초안.md](제안방법_초안.md) |
| 실험 결과 전체 | [04_실습/05_libero/README.md](04_실습/05_libero/README.md) |
| 날짜별 기록 | [03_일지/](03_일지/) |
| 논문 정리 | [02_논문노트/](02_논문노트/) |
| 모르는 말 | [용어사전.md](용어사전.md) |
| 설치 | [실행방법.md](04_실습/05_libero/실행방법.md) |

## 7-3. 태스크 번호 (LIBERO-Goal, 10개가 같은 장면을 씁니다)

| 번호 | 지시 | 물체를 쥐나 |
|---|---|---|
| 0 | 가운데 서랍 열기 | ✗ |
| 1 | 그릇을 스토브 위에 | ✅ |
| 2 | 와인병을 찬장 위에 | ✅ |
| 3 | 위 서랍 열고 그릇 넣기 | ✅ |
| 4 | 그릇을 찬장 위에 | ✅ |
| 5 | 접시 밀기 | ✗ |
| 6 | 크림치즈를 그릇에 | ✅ |
| 7 | 스토브 켜기 | ✗ |
| 8 | 그릇을 접시에 | ✅ |
| 9 | 와인병을 선반에 | ✅ |

⚠️ **A(중단되는 태스크)는 물체를 쥐는 것이어야** `grasp:3` 시점에 전환할 수 있습니다.
주로 8·4·1·2 를 씁니다.

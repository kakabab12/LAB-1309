# 지금 어디까지 왔나 · 다른 PC에서 이어받기

> **마지막 갱신: 2026-09-21 (월)**
>
> 다른 PC에서 처음 보신다면 **이 문서만 읽으면 이어서 일할 수 있게** 써 두었습니다.
> 설치는 [실행방법.md](04_실습/05_libero/실행방법.md), 연구 배경은 [연구주제.md](연구주제.md).

---

## 1. 한 문단 요약

로봇이 **물체를 쥔 채** 새 지시를 받으면 잘 못 따릅니다(32%). 초기 자세로 되돌리면 89%까지 오르지만
**그건 리셋이라 금지**입니다. 8일 동안 15가지 방법을 시도했는데 **제약을 지키면서 32%를 넘은 것이 없습니다.**

대신 **원인을 찾았습니다**: 물체를 쥐는 순간 정책이 **지시문에 귀를 닫습니다** (CMI 0.719 → 0.284, −61%).
지금은 그 "귀"를 여는 방법을 찾고 있습니다.

## 2. ⛔ 절대 제약 (이걸 어기면 연구가 무의미해집니다)

- 전환할 때 **초기 자세로 복귀 금지** — 캘리브레이션/리셋이 되어 버립니다
- **사람처럼 자연스럽게** 이어져야 합니다 (멈췄다 가는 stop-and-go 금지)
- 물체를 아무 데나 내려놓는 것은 **허용**
- `retreat` / `ret_pos` / `ret_rot` 은 **진단·상한 측정 전용**입니다.
  성능이 좋다고 이것을 방법으로 쓰거나, 학습으로 따라하게 하면 안 됩니다

## 3. 현재 성적표

전환 시점 = 물체를 쥔 직후. 4쌍(8→7, 4→7, 1→5, 2→5) × 10회.

| 전략 | B 성공 | A+B 둘 다 | 제약 |
|---|---|---|---|
| retreat (초기자세 복귀) | **89%** | **56%** | ⚠️ **위반** — 상한 참고용 |
| ret_pos (위치만 복귀) | 64% | 5% | ⚠️ 위반 |
| bon (후보 8개 중 선택) | 34% | 0% | ✅ |
| **flush (기준선)** | **32%** | **0%** | ✅ |
| keep | 29% | 0% | ✅ |
| blend | 25% | 0% | ✅ |
| rollback | 19% | 0% | ✅ |
| rtc | 18% | 1% | ✅ |
| LoRA 1차 / 2차 / 3차 | 32 / 31 / 30% | 0% | ✅ |

**제약을 지키는 것 중 기준선 32%를 의미 있게 넘은 것이 없습니다. A 재개는 전부 0%입니다.**

## 4. ⭐ 지금까지 알아낸 것 (중요한 순서대로)

### ① 물체를 쥐면 지시문이 안 들린다 (2026-09-21, 가장 중요)

| 측정 지점 | CMI (지시문이 동작을 가르는 정도) |
|---|---|
| 에피소드 시작 | 0.719 |
| **물체를 쥔 직후** | **0.284 (−61%)** |
| 들고 이동 중 | 0.761 (회복) |

태스크 4개 모두 같습니다. → [보고서](04_실습/05_libero/steerability/README.md)

**지금까지 실패한 대책들이 전부 "몸"(자세·노이즈)을 고치려 했습니다. 문제는 "귀"였습니다.**

문헌 확인 결과 **이 측정은 아무도 한 적이 없습니다** —
"VLA가 말을 잘 안 듣는다"는 알려져 있지만([LIBERO-PRO](02_논문노트/LIBERO-PRO.md)),
**에피소드 안에서 시점별로** 잰 사람은 없습니다.

### ② 실패의 정체는 policy idling

물체는 100% 놓고, B 경로의 60~80%까지 가서 **좁은 영역에서 왕복**합니다. 실패의 80%.
→ [보고서](04_실습/05_libero/failure_mode/README.md)

### ③ A 재개가 0%인 이유는 규명됨

| 시점 | 손목 회전 | 위치 |
|---|---|---|
| 전환 (B 시작) | 4.9도 | 24.6cm |
| **재개 (A2 시작)** | **83.6도** | **32.5cm** |

두 축이 동시에 범위 밖이라, 하나만 고쳐서는 안 됩니다. → [보고서](04_실습/05_libero/boundary/README.md)

### ④ 노이즈가 결과를 크게 가르지만 고를 수가 없다

같은 상태에서 노이즈만 바꿔 20% ↔ 90%. 그런데 **좋은 노이즈는 장면마다 다릅니다.**
→ [보고서](04_실습/05_libero/noise_seed/README.md)

## 4-2. ⏳ 지금 보고 있는 것 — 지시문 증폭 (첫 신호)

CMI 가 떨어지는 것이 문제라면 **그 채널을 키우면** 된다는 발상입니다.

```
v = v_B + (w − 1) · (v_B − v_A)

  v_A : 이전 지시 A 로 구한 속도장   ← 음의 조건
  v_B : 새 지시 B 로 구한 속도장     ← 양의 조건
  w=1 이면 원래 정책과 비트 단위로 동일 (검증 완료)
```

이전 지시를 음의 조건으로 쓰는 것이 핵심입니다 — **전환 문제에서는 버려야 할 지시가 무엇인지
우리가 알고 있습니다.** 자세를 안 건드려 제약도 지킵니다.

### 첫 결과 (2026-09-21, 4쌍 × 10회)

| 지표 | 기준 | **w=1.5** | w=2.0 | p (w=1.5) |
|---|---|---|---|---|
| B 성공률 | 25% | 33% | 17% | 0.302 |
| **맴돌기 비율** | **68%** | **45%** | 67% | **0.035** ✅ |
| jerk (자연스러움) | 8.97 | 10.01 | — | 1.12배 (허용) |

**성공률로는 안 보이던 것이 맴돌기로는 보입니다.**
실패의 80%가 맴돌기이므로 같은 현상의 더 민감한 측정입니다
(성공률은 0/1 이라 10회로 53%p 미만을 못 보지만, 맴돌기는 에피소드마다 연속적으로 잽니다).

⚠️ **w=2.0 은 해롭습니다.** 맴돈 에피소드 수는 그대로인데
**맴돈 시간 비중이 14% → 35%** 로 늘었습니다. 한 번 빠지면 더 오래 갇힙니다.
동작 크기가 1.11 로 포화 구간에 들어간 탓으로 보입니다.

**아직 결론이 아닙니다** — 맴돌기 하나가 줄었을 뿐 성공으로 이어지지 않았습니다.
⏳ 증폭이 실제로 CMI 를 올리는지(`run_cfg_cmi.sh`)를 보고 판단합니다.

## 5. ❌ 닫힌 길 (다시 열지 마세요)

| 방향 | 최종 결과 |
|---|---|
| 노이즈 — 좋은 시드 고정 | 처음 보는 쌍에서 **7%** (기준 31%, p=0.031) |
| 노이즈 — CEM 으로 평균이동 학습 | 검증 50회 **−10%p** (p=0.885) |
| 노이즈 — 크기 조절 (σ 0.5~2.0) | σ=2.0 에서 전부 0% |
| 노이즈 — 판정기로 후보 선택 | Q−V = 0.0003 (동작을 알려줘도 예측이 안 나아짐) |
| 학습 — LoRA 1·2·3차 | 32 / 32 / 31 / 30% |
| 회복 — 흔들기 4방향 · rollback · finish_a | 전부 기준선 이하 |

## 6. ⚠️ 통계 한계 — 모든 주장에 붙여야 하는 단서

조건당 10회로는 **53%p 미만을 구분할 수 없습니다.**

| 표본 | 검출 가능한 최소 효과 |
|---|---|
| 단일 쌍 10회 | 53%p |
| 4쌍 합계 36회 | 28%p |
| 4쌍×2시점 72회 | 20%p |

**우리의 "효과 없음"은 전부 "큰 효과 없음"입니다.**
실제로 CEM 이 10회에서 +30%p 로 보였다가 20회에서 −20%p 로 뒤집혔습니다.
→ 작은 표본의 큰 값을 믿지 마세요.

## 7. 지금 돌고 있는 것 (연구실 PC, 2026-09-21 12:40 기준)

GPU가 1개라 **순서대로 이어 달리게** 걸어 두었습니다.

| # | 스크립트 | 무엇을 정하나 | 로그 |
|---|---|---|---|
| 1 | `run_cfg.sh` | 지시문 증폭 w=1.5/2/3 이 전환을 개선하는가 | `outputs/cfg.log` |
| 2 | `run_cfg_cmi.sh` | **증폭이 실제로 CMI를 올리는가** ← 제일 중요 | `outputs/cfg_cmi.log` |
| 3 | `run_cmi_curve.sh` | 귀가 언제 닫히고 언제 열리는가 (시간 곡선) | `outputs/cmi_curve.log` |
| 4 | `run_manypairs.sh` | 쌍을 4개 → 20개로 (표본·난이도 원인) | `outputs/manypairs.log` |
| 5 | `run_repeat.sh` | 대안: 지시문 토큰 반복 | `outputs/repeat.log` |

**확인하는 법**

```bash
cd ~/smolVLA
ps -eo pid,args | awk '$2=="bash" && /run_/{print $1, $3}'   # 무엇이 돌고 있나
grep -E "^===|^SUMMARY" outputs/cfg.log                       # 진행 상황
```

## 8. 결과가 나오면 무엇을 보나

```bash
cd ~/smolVLA
.venv/bin/python analyze_cfg.py          # 증폭: 성공률 + jerk + 실패유형
.venv/bin/python compare_steer.py outputs/steer_base9 outputs/steer_cfg15 outputs/steer_cfg30
.venv/bin/python cmi_curve.py outputs/steer_curve
.venv/bin/python pair_steer.py outputs/steer_curve
```

**⚠️ 성공률만 보고 판단하지 마세요.** 증폭은 동작을 키웁니다.
성공률이 올라도 jerk 가 같이 오르면 "사람처럼 자연스럽게" 제약을 깹니다.
`analyze_cfg.py` 가 둘을 항상 같이 찍습니다.

단, **jerk 하나로도 판단하면 안 됩니다** — 제약을 어기는 리셋 계열이 오히려 가장 부드럽습니다
(retreat 3.63 vs flush 9.59). jerk + 초기자세까지의 거리 + 경로 길이를 **함께** 보세요.
→ [자연스러움 보고서](04_실습/05_libero/naturalness/README.md)

## 9. 갈림길 — 결과별로 무엇을 하나

| 2번(CMI) 결과 | 뜻 | 다음 |
|---|---|---|
| CMI ↑ , 성공률 그대로 | 귀는 열렸는데 행동이 안 따라옴 | **채널이 병목이 아니다** → 가설 수정 |
| CMI 그대로 | 증폭이 채널을 못 키움 | 토큰 반복(5번) → attention 조정 → CMI 목적함수 학습 |
| CMI ↑ , 성공률 ↑ | ✅ 방법이 통한다 | 쌍 20개로 확대 검증, 자연스러움 정밀 측정 |

## 10. 하드웨어 — GPU를 바꾸면 빨라지나 (2026-09-21 실측)

**실험 속도는 거의 안 빨라집니다.**

| | 비중 |
|---|---|
| 정책 추론 (GPU) | 38% |
| **시뮬레이션·렌더링 (CPU)** | **62%** |

RTX 4090 으로 바꿔도 **1.46배**가 전부입니다. LIBERO 시뮬레이션이 CPU에서 돌기 때문입니다.
→ **병렬 실행(3~4배)이 훨씬 낫고 돈이 안 듭니다.** 1080 Ti 11GB 에 SmolVLA 는 ~3GB 라
동시에 3개를 돌릴 수 있습니다 (CPU 8코어 기준).

**다만 실물 로봇(PiPER)에는 GPU가 필수입니다.**
1080 Ti 단독 추론이 **560ms = 11.2스텝**인데, 저희 측정으로 **0.5초 지연이면 성공률 90%→12%** 입니다.
→ [지연 실험](04_실습/05_libero/latency/README.md)

## 11. 교훈 (같은 실수를 반복하지 않기 위해)

1. **돌아가는 bash 스크립트를 덮어쓰지 마세요** — bash 는 파일을 조금씩 읽습니다
2. `pgrep -f` 는 자기 명령줄까지 잡습니다. `ps -eo pid,args` 로 확인하세요
3. **한 축만 재고 결론짓지 마세요** — "회전 30도 절벽"이 조합 조건에 한 시간 만에 뒤집혔습니다
4. **예측을 먼저 적어 두세요** — 판정 기준까지 스크립트에 넣으면 데이터가 오는 즉시 결론이 납니다
5. **자동 판정에 유의성 조건을 넣으세요** — 점추정만 보고 진행해 주말을 닫힌 길에 썼습니다
6. **작은 표본의 +30%p 를 믿지 마세요** — 20회에서 −20%p 로 뒤집혔습니다
7. **가설을 검증할 때 집단이 맞는지 먼저 보세요** — 정상 궤적으로는 "틀어진 손목이 고쳐지는가"를 못 잽니다
8. **새 방법은 항등 검사부터** — 파라미터를 중립값으로 두면 원본과 **같아야** 합니다.
   안 그러면 나중 차이가 효과인지 버그인지 모릅니다 (실제로 상쇄 오차가 있었습니다)

## 12. 주요 파일

| 파일 | 내용 |
|---|---|
| `switch_experiment.py` | 전환 실험 본체 |
| `instr_cfg.py` | 지시문 증폭 (이전 지시를 음의 조건으로) |
| `test_cfg_identity.py` | w=1 항등 검사 |
| `steerability.py` / `analyze_steer.py` / `compare_steer.py` | CMI 측정·비교 |
| `cmi_curve.py` / `pair_steer.py` | CMI 시간 곡선 · 쌍별 분리도 |
| `analyze_cfg.py` | 증폭 평가 (성공률 + jerk) |
| `rotation_at_resume.py` / `rotation_vs_travel.py` | 손목 회전 분석 |
| `pose_sensitivity.py` | 자세 교란 |
| `analyze_lora_versions.py` | 원본/1차/2차/3차 비교 |
| `oracle_bon.py` / `cem_noise.py` | (닫힌 길) 오라클 · 노이즈 최적화 |

사본은 [04_실습/05_libero/코드/](04_실습/05_libero/코드/) 에도 있습니다 — 파일별 설명도 거기 있습니다.

## 13. 일정에 대한 정직한 말

**"언제 모델이 완성되나"에 날짜를 드릴 수 없습니다.** 8일 동안 15가지를 시도했고 전부 기준선 근처입니다.
그리고 오라클로 같은 상태를 16번 다시 뽑아도 **42%의 상태는 어떤 선택으로도 실패**합니다 — 상한이 낮을 수 있습니다.

대신 산출물을 둘로 나누면 이렇습니다.

| 산출물 | 상태 |
|---|---|
| **진단·측정 결과** (CMI 발견, 실패 기제, 경계 측정) | 거의 완성. 정리에 약 1주 |
| **작동하는 방법** (제약 지키며 32% 넘기) | 근거 없음. 약속 불가 |

**중단 기준을 미리 정해 두었습니다**: 10/2(금)까지 20쌍에서 기준선을 넘는 방법이 하나도 없으면,
방법 추구를 멈추고 **진단 중심으로 정리**합니다. 그래야 10월 초에 내놓을 것이 남습니다.

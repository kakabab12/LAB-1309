# 용어 사전

논문을 읽다가 막히면 여기서 찾아보세요. 새 용어를 만나면 계속 추가하세요.

## 딥러닝

| 용어 | 뜻 |
|---|---|
| **텐서(tensor)** | 다차원 배열. 모든 데이터의 기본 형태 |
| **shape** | 텐서의 모양. 예: (배치, 채널, 높이, 너비) |
| **배치(batch)** | 한 번에 처리하는 데이터 묶음 |
| **에폭(epoch)** | 전체 데이터를 한 바퀴 도는 것 |
| **스텝(step)** | 가중치를 한 번 갱신하는 것 |
| **학습률(learning rate)** | 한 번에 얼마나 크게 고칠지 |
| **손실(loss)** | 정답과의 차이. 이걸 줄이는 게 학습 |
| **과적합(overfitting)** | 학습 데이터만 잘하는 상태 |
| **정규화(normalization)** | 값의 범위를 고르게 맞추는 것 |
| **사전학습(pretrain)** | 큰 데이터로 미리 학습해둔 것 |
| **파인튜닝(fine-tuning)** | 사전학습 모델을 내 데이터로 조금 더 학습 |
| **LoRA** | 작은 행렬만 학습해 메모리를 아끼는 파인튜닝 |
| **동결(freeze)** | 일부 층을 학습하지 않게 고정 |
| **gradient accumulation** | 작은 배치를 여러 번 모아 큰 배치 효과 |
| **fp32 / fp16 / bf16** | 숫자 정밀도. 낮을수록 메모리↓ |
| **체크포인트** | 저장된 모델 가중치 |

## Transformer / VLM

| 용어 | 뜻 |
|---|---|
| **토큰(token)** | 입력을 자른 단위 (단어 조각, 이미지 패치) |
| **임베딩** | 토큰을 벡터로 바꾼 것 |
| **attention** | 어떤 토큰이 어떤 토큰을 참고할지 정하는 계산 |
| **Q, K, V** | Query(질문), Key(열쇠), Value(내용) |
| **cross-attention** | 다른 종류 정보를 참고하는 attention |
| **자기회귀(autoregressive)** | 한 토큰씩 차례로 생성. 느림 |
| **백본(backbone)** | 기반이 되는 큰 모델 |
| **VLM** | 이미지와 언어를 함께 다루는 모델 |
| **CLIP / SigLIP** | 이미지와 텍스트를 같은 공간에 놓는 모델 |

## 생성 모델

| 용어 | 뜻 |
|---|---|
| **Diffusion** | 노이즈를 점점 걷어내며 결과를 만드는 방식 |
| **denoising step** | 걷어내는 횟수. 많을수록 품질↑ 속도↓ |
| **Flow Matching** | 노이즈→정답 경로를 직선에 가깝게 배우는 방식. 더 빠름 |
| **다중 모드(multi-modal)** | 정답이 여러 개일 수 있는 상황 |

## 로봇

| 용어 | 뜻 |
|---|---|
| **DOF(자유도)** | 독립적으로 움직이는 축의 수 |
| **엔드 이펙터** | 팔 끝의 도구(그리퍼) |
| **관절 공간** | 관절 각도로 표현 |
| **작업 공간** | 끝단 위치·방향으로 표현 |
| **FK(순기구학)** | 관절값 → 끝단 위치 |
| **IK(역기구학)** | 끝단 위치 → 관절값 |
| **특이점(singularity)** | 관절 속도가 튀는 위험한 자세 |
| **오일러각 / 쿼터니언 / 축-각** | 회전을 표현하는 방법들 |
| **delta 명령** | 절대 좌표가 아니라 "지금에서 얼마만큼" |
| **제어 주기** | 초당 명령 횟수(Hz) |
| **teleoperation** | 사람이 원격으로 조종 |
| **리더-팔로워** | 사람이 잡는 팔(리더)을 따라 로봇(팔로워)이 움직임 |

## 로봇 학습

| 용어 | 뜻 |
|---|---|
| **모방학습** | 사람 시연을 따라 배우기 |
| **Behavior Cloning** | 관측→동작을 지도학습으로 흉내내기 |
| **공변량 이동** | 학습에 없던 상황에 빠져 실패가 커지는 문제 |
| **Action Chunk** | 여러 스텝의 동작을 한 번에 예측한 묶음 |
| **n_action_steps** | chunk 중 몇 개를 실행하고 다시 추론할지 |
| **temporal ensembling** | 겹치는 예측을 평균내 부드럽게 하기 |
| **rollout** | 정책을 한 에피소드 실행하는 것 |
| **성공률** | 성공한 에피소드 비율 |
| **jerk** | 가속도의 변화율. 동작이 튀는 정도 |
| **embodiment** | 로봇 몸체의 종류 |
| **VLA** | 이미지+언어→동작을 출력하는 모델 |
| **action expert** | 동작 생성만 담당하는 작은 모듈 |
| **비동기 추론** | 추론과 제어 루프를 분리해 돌리는 방식 |

## 태스크 전환 실험 (우리 연구)

자세한 설명과 실측값은 [2026-09-14 일지](03_일지/2026-09-14.md) 참고.

| 용어 | 뜻 |
|---|---|
| **태스크 전환(task switching)** | 환경 리셋 없이 수행 도중 지시문만 다른 태스크로 바꾸는 것 |
| **재개(resumption)** | 끼어든 태스크를 끝낸 뒤 원래 하던 태스크를 다시 수행하는 것 |
| **flush** | 전환 시 남은 action chunk를 버리고 즉시 새 지시로 재추론 |
| **keep** | 남은 chunk를 끝까지 실행한 뒤 새 지시로 넘어감 |
| **blend** | 이전 chunk와 새 chunk의 겹치는 앞부분을 가중평균으로 섞음 |
| **retreat** | 쥔 물체를 내려놓고 초기 자세로 복귀한 뒤 전환 (스크립트 동작) |
| **전환 시점** | 접근 중 / 잡은 직후 / 들고 이동 중 — 언제 지시를 바꿨는가 |
| **반응 시간** | 지시 변경 후 궤적이 "안 바꿨을 때"에서 2cm 벌어지기까지의 스텝 수 |
| **goal predicate** | LIBERO가 성공을 판정하는 BDDL 조건식 |
| **초기 자세(home pose)** | 에피소드 시작 시 팔 자세. 학습 데모의 출발점 |

## 도구 · 환경

| 용어 | 뜻 |
|---|---|
| **LeRobot** | Hugging Face의 로봇 학습 라이브러리 |
| **LIBERO** | 로봇 조작 시뮬레이션 벤치마크 |
| **MuJoCo** | 물리 시뮬레이터 |
| **CALVIN** | 지시를 연속으로 수행하는 벤치마크 |
| **OXE** | 여러 로봇 데이터를 모은 대규모 데이터셋 |
| **compute capability** | GPU 세대를 나타내는 번호 (1080 Ti는 6.1) |
| **CUDA** | NVIDIA GPU 연산 플랫폼 |
| **SocketCAN** | 리눅스의 CAN 통신 방식 |
